static class Utils {
  /*
  static PImage getBackground() {
  noStroke();  
  resetMatrix();
  float maxr = max(width, height)*1.41;
  for(float i=0; i<1; i+=0.01) {
    float r = map(i*i, 0, 1, maxr, 20);
    float co = map(i, 0, 1, 120, 250);
    fill(co);
    ellipse(width/2, height/2, r, r);
  }
  for(int i=0; i<500; i++) {
    fill(random(255), 2);
    float r = 503 - i; //random(3, 100);
    ellipse(random(width), random(height), r, r);
  }
}
*/
}
